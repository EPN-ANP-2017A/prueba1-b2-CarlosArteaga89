{% extends 'base.html.twig' %}

{% block body %}
    <h1>Registrar Pedido</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Guardar" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('pedido_index') }}">Lista de Pedidos</a>
        </li>
    </ul>
{% endblock %}
