{% extends 'base.html.twig' %}

{% block body %}
    <h1>Registrar Plato</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('plato_index') }}">Lista de Platos</a>
        </li>
    </ul>
{% endblock %}
